# general cfg
image_flip:             False
max_depth:              15
domain:                 depth

# ros topics
vizTopic:               /viplanner/viz_path_depth
imgTopic:               /camera/depth/image_rect_raw
infoTopic:              /camera/depth/camera_info
goalTopic:              /mp_waypoint
pathTopic:              /viplanner/path

# frame ids
robot_frame:            base
odom_frame:             odom
